import 'package:flutter/material.dart';

class albums extends StatelessWidget {
  const albums({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(height: 200,width: 200,color: Colors.green,),
    );
  }
}